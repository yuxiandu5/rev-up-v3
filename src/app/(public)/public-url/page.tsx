import { useEffect } from "react";

export default function PublicUrlPage() {
  // useEffect(() => {
  //   const publicUrl = `${window.location.origin}/public-url`;

  // }, []);

  return <div>Public URL copied to clipboard</div>;
}
